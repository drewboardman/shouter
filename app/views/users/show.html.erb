<div class="row">
	<div class="col-lg-4"> 
		<%= render @user %>
		<% if current_user.can_follow?(@user) %>
			<%= follow_button @user %>	
		<% end %>
	</div>
	<div class="col-lg-8"> 
		<%= render @shouts %>
	</div>
</div>